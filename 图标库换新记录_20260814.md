# 图标库换新记录（2026-08-14）

> 本次操作：以 `icon新` 目录（183 个新绘制图标）为唯一准源，替换/新增/删除设计系统图标库，多端（Web/移动）对齐。
> 新图规格：24×24 · 固定线宽 1.8 · 纯线性（fill none + stroke currentColor）· round。

---

## 一、操作汇总

| 动作 | 数量 | 说明 |
|---|---|---|
| 🔁 替换 | 180 | 新图替换同语义旧图（Web + 移动同源） |
| ➕ 新增 | 3 | `email` / `key` / `energy_save2`（HEAD 无对应语义） |
| 🗑 删除 | 17 | 新库未覆盖的业务旧图标（双端） |
| 🛡 框架必需保留 | 4 | `sun` / `moon` / `close` / `language`（顶栏必需，列为待补绘） |

**替换后库规模**：Web = 185 个 · 移动 = 184 个（两端同源对齐）

---

## 二、删除的图标（新库未覆盖，17 个）

> 以下语义新库未提供，已从双端库删除；如需保留请列入下方待补绘。

| 语义名 | 中文用途（推断） |
|---|---|
| `add_item` | 新增条目 |
| `basic` | 基础资料 |
| `billing_manage` | 计费管理 |
| `custom_report` | 自定义报表 |
| `customer_manage` | 客户管理 |
| `device_activate` | 设备开通 |
| `device_activate_alt` | 设备开通（变体） |
| `device_capacity_monitor` | 设备产能监控 |
| `device_status_board` | 设备状态看板 |
| `done` | 完成 |
| `energy_save` | 节能 |
| `meter` | 电表 |
| `mold` | 模具 |
| `monitor` | 监控 |
| `repair_maintenance` | 维修保养 |
| `report_export` | 报表导出 |
| `system_design` | 系统设计 |

---

## 三、框架必需保留图标（新库未覆盖，列为待补绘）

> 这些是 page-template 顶栏/框架必需，本次**保留未删**，但新库没有对应新图。建议后续补绘（当前沿用旧版线性图）。

| 语义名 | 用途 |
|---|---|
| `sun` | 顶栏亮色切换 |
| `moon` | 顶栏暗色切换 |
| `close` | 关闭 |
| `language` | 多语言切换（Web） |

---

## 四、新增图标（3 个）

| 语义名 | 用途 |
|---|---|
| `email` | 邮件/消息 |
| `key` | 密钥/授权 |
| `energy_save2` | 节能（新命名） |

---

## 五、待补绘清单（你后续绘制这些来补充库）

> 备注：主要包含 17 个已删业务图标 + 4 个框架必需图标；若某些语义不再需要，可忽略。

- 已删待补：`add_item` `basic` `billing_manage` `custom_report` `customer_manage` `device_activate` `device_activate_alt` `device_capacity_monitor` `device_status_board` `done` `energy_save` `meter` `mold` `monitor` `repair_maintenance` `report_export` `system_design`
- 框架必需待补：`sun` `moon` `close` `language`
