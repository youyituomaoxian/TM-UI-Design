# 弘讯设计系统 · 标准提示词模板（PROMPTS）

> 用法：选一段复制 → 只替换 `<…>` 占位符 → 发给 Agent。
> 规则细节交给 Agent 自己读（AGENTS.md / GENERATION-SOP / RULES / 门禁 0 HIGH 兜底），提示词只给「任务 + 入口」。
> **先选级**：Agent 会不会自动加载 AGENTS.md？会 → 用一级极简版；不会 → 用二级完整版。

---

## 一级 · 极简版（默认，日常最常用）

**适用**：codex / Claude Code / Reasonix / Cursor 等**在克隆目录里对话**的 Agent——
`AGENTS.md`（或 `CLAUDE.md`）自动加载，它自己知道跑脚手架、读规范、过门禁，不需要你教。

```
用弘讯设计系统生成「<一句话需求>」页面。
```

**只需替换 1 处：**

| 占位符 | 填什么 | 例 |
|---|---|---|
| `<一句话需求>` | 你要的页面（含关键模块） | 设备监控大屏，4 张 KPI 图标卡（设备总数/开机率/故障数/今日产量）+ 设备表格（数值列右对齐）+ 近 7 日产能柱状图 |

**填好的示例（直接改需求就能用）：**

> 用弘讯设计系统生成「设备监控大屏，4 张 KPI 图标卡（设备总数/开机率/故障数/今日产量）+ 设备表格（数值列右对齐）+ 近 7 日产能柱状图」页面。

> 用弘讯设计系统生成「移动端登录页：顶部导航 + 手机号输入 + 验证码 + 登录按钮」页面。

> 用弘讯设计系统生成「工单管理页：筛选 + 表格 + 分页 + 新建工单表单弹窗」页面。

---

## 二级 · 完整版（外部 Agent / 不确定时用）

**适用**：网页版 AI、临时工具、批量派发等**不会自动加载 AGENTS.md** 的 Agent——
必须在提示词里给出「任务 + 入口」（命令 + 规范路径 + 门禁），否则它会自由发挥、风格漂移。

```
用弘讯设计系统生成「<一句话需求>」：node <仓库根>/scripts/new-page-<端>.js <语义名>，
遵循 GENERATION-SOP.md 与 <仓库根>/弘讯<端>端design-system/RULES.md，门禁 0 HIGH 才交付。
```

**只需替换 4 处（其中 2 处每次几乎一样）：**

| 占位符 | 填什么 | 例 |
|---|---|---|
| `<一句话需求>` | 你要的页面（含关键模块）——**每次改** | 设备监控大屏，4 张 KPI 图标卡 + 设备表格 + 产能柱状图 |
| `<语义名>` | 页面英文名（小写短横线）——**每次改** | device-monitor |
| `<端>` | `web` 或 `mobile` | web |
| `<仓库根>` | 设计系统目录 | D:\design-system |

**填好的示例：**

> 用弘讯设计系统生成「设备监控大屏，4 张 KPI 图标卡（设备总数/开机率/故障数/今日产量）+ 设备表格（数值列右对齐）+ 近 7 日产能柱状图」：node D:\design-system\scripts\new-page-web.js device-monitor，遵循 GENERATION-SOP.md 与 D:\design-system\弘讯web端design-system/RULES.md，门禁 0 HIGH 才交付。

**端 / 场景差异（写在需求里即可，Agent 自己处理）：**
- 移动端：`<端>` 换 `mobile`，RULES 换 `弘讯移动端design-system/`；底部导航 3-5 项随需求增减，登录/详情页无底部导航合法。
- 看板大屏（实时图表）：需求里写明「实时数据 2 秒刷新 + 暂停开关」；图表遵循 CHART-SPEC.md（Agent 自动读）。
- 非 B 端页面（营销/门户/大屏可视化）：不需要脚手架，直接按需求自由设计（不套 B 端框架）。

---

## 专用模板（两级都适用；发给认 AGENTS.md 的 Agent 时可去掉命令细节）

### 审计 / 修复既有页面

```
对 <页面路径> 做规范审计并修复：跑 node <仓库根>/弘讯<端>端design-system/validate-spec.js <页面路径> 修到 0 HIGH；
规范/门禁自身缺陷上报维护者（记录到两端 CHANGELOG.md），不擅改 validate-spec.js。
```

> 例：对 D:\my-project\output\device-monitor.html 做规范审计并修复：跑 node D:\design-system\弘讯web端design-system\validate-spec.js D:\my-project\output\device-monitor.html 修到 0 HIGH；规范/门禁自身缺陷上报维护者（记录到两端 CHANGELOG.md），不擅改 validate-spec.js。

### 品牌换肤（换品牌色 / 暗色）

```
把 <页面/项目> 的品牌色换成 <hex>：node <仓库根>/brand-color-engine/generate.js <hex> light <web|mobile> 与 dark 各跑一次，
生成亮/暗双模式 :root 覆盖块追加，组件 CSS 保持 var(--*) 不动，门禁 0 HIGH。
```

> 例：把 D:\my-project\output 下页面的品牌色换成 #2E7D5B：node D:\design-system\brand-color-engine\generate.js 2E7D5B light web 与 dark 各跑一次，生成亮/暗双模式 :root 覆盖块追加，组件 CSS 保持 var(--*) 不动，门禁 0 HIGH。

### 存量系统弘讯化迁移

```
把 <现有项目> 迁移到弘讯体系（品牌 #005EAE，亮暗双模式）：先摸底（技术栈/页面数/裸 hex 率）→
注入弘讯 :root tokens（含暗色）→ 逐页对齐（颜色→var(--*) 优先、结构→弘讯组件类，每页门禁 0 HIGH）→ 防回漂（禁新裸 hex）。
```

> 例：把 D:\legacy-system 迁移到弘讯体系（品牌 #005EAE，亮暗双模式）：先摸底（技术栈/页面数/裸 hex 率）→ 注入弘讯 :root tokens（含暗色）→ 逐页对齐（颜色→var(--*) 优先、结构→弘讯组件类，每页门禁 0 HIGH）→ 防回漂（禁新裸 hex）。

### 批量派发（多页分给多个 Agent）

```
按弘讯设计系统批量生成 <N> 个页面：每页任务书 = 完整版模板 + 该页需求清单（仓库根/端/门禁命令每份都带）；
页面间禁互相参照 DOM；全部 0 HIGH 后统一 node <仓库根>/ci-local.js 验收。
```

> 例：按弘讯设计系统批量生成 5 个页面：每页任务书 = 完整版模板 + 该页需求清单（仓库根/端/门禁命令每份都带）；页面间禁互相参照 DOM；全部 0 HIGH 后统一 node D:\design-system\ci-local.js 验收。

---

## 附 · 脚手架命令速查（维护者 / 手动验证用）

| 端 | 命令 | 落盘 |
|---|---|---|
| Web | `node <仓库根>/scripts/new-page-web.js <语义名>` | `<当前目录>/output/<语义名>_<YYYYMMDD>_<HHmm>.html` |
| 移动 | `node <仓库根>/scripts/new-page-mobile.js <语义名>` | 同上 |

> 脚手架自动完成：克隆 page-template 框架 → 注入克隆凭证 meta → 修正 CSS link → 防覆盖命名（同名自动加时间戳）。
