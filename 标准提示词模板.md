# 弘讯设计系统 · 标准提示词模板（PROMPTS）

> 规则细节交给 Agent 自己读（SOP / RULES / 门禁 0 HIGH 兜底），提示词只给**任务 + 入口**。
> 用法：选一段复制，替换 `<占位符>`，发给 Agent。
> `<仓库根>` = 含 `ci-local.js` 的目录或 `$env:TM_DESIGN_REPO`。

---

**1 · Web B 端页面生成**

```
按弘讯设计系统生成 <页面描述>：用脚手架起步 node <仓库根>/scripts/new-page-web.js <语义名>，
遵循 <仓库根>/GENERATION-SOP.md 与 弘讯web端design-system/RULES.md，门禁 0 HIGH 才交付。
```

**2 · 移动端页面生成**

```
按弘讯设计系统生成 <移动端页面描述>：用脚手架起步 node <仓库根>/scripts/new-page-mobile.js <语义名>，
遵循 <仓库根>/GENERATION-SOP.md 与 弘讯移动端design-system/RULES.md，门禁 0 HIGH 才交付。
（底部导航 3-5 项随需求增减；登录/详情等无 BottomNav 合法。）
```

**3 · 既有页面审计 / 修复**

```
对 <页面路径> 做规范审计并修复：跑 node <对应端>/validate-spec.js <页面> 逐条修到 0 HIGH；
规范/门禁自身缺陷写 BLOCKED.md，不擅改 validate-spec.js。
```

**4 · 品牌换肤 / 暗色**

```
把 <页面/项目> 的品牌色换成 <hex>，亮/暗：<亮|暗|都要>——运行
node <仓库根>/brand-color-engine/generate.js <hex> <light|dark> <web|mobile> 生成 :root 覆盖块追加，
组件 CSS 保持 var(--*) 不动，门禁 0 HIGH。
```

**5 · 存量系统弘讯化迁移**

```
把 <现有项目> 的视觉迁移到弘讯体系（品牌 #005EAE，亮暗双模式）：先摸底（技术栈/页面数/裸 hex 率），
再注入弘讯 :root tokens（含暗色），逐页对齐（颜色→var(--*) 优先、结构→弘讯组件类，每页门禁 0 HIGH），
防回漂（禁新裸 hex）。
```

**6 · 看板 / 数据大屏（含实时图表）**

```
按弘讯设计系统生成 <看板描述>（实时数据 2 秒刷新 + 暂停开关）：脚手架起步 node <仓库根>/scripts/new-page-web.js <语义名>，
遵循 GENERATION-SOP.md + RULES.md + CHART-SPEC.md；报警/日志列表定高滚动（.scroll-fixed），门禁 0 HIGH。
```

**7 · 批量派发（多页面分给多个 Agent）**

```
按弘讯设计系统批量生成 <N> 个页面：每页任务书 = 对应单页模板 + 该页内容清单（仓库根/端/门禁命令每份都带）；
页面间禁互相参照 DOM；全部 0 HIGH 后统一 node ci-local.js 验收。
```

---

### 附 · 脚手架命令速查

| 端 | 命令 | 默认落盘 |
|---|---|---|
| Web | `node <仓库根>/scripts/new-page-web.js <语义名>` | cwd/output/ |
| 移动 | `node <仓库根>/scripts/new-page-mobile.js <语义名>` | cwd/output/ |

> 脚手架自动完成：克隆 page-template.html 框架 → 注入克隆凭证 meta → 修正 CSS link → 防覆盖命名。
