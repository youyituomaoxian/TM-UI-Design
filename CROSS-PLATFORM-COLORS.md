# 跨端中性色契约（CROSS-PLATFORM-COLORS）— 2026-08-07

> 解决 GAP-05：Web 与移动端中性色 N1-N10 **同名但色值分治**（平台色板差异为有意设计），
> 本契约让差异明确、语义一致，避免 Agent 跨端误用。
> 语义定义以本表为准；色值以各端 `template.css` 真源为准（改色值改真源，本表同步）。

## 命名语义（两端一致，跨端通用）

| Token | 语义 | 典型用途 | 注意 |
|-------|------|----------|------|
| --n1 | surface 基础面 | 页面/卡片主背景 | 亮=白，暗=#1E293B |
| --n2 | surface 轻量 | 次级背景/悬浮面 | 两端差异（平台质感） |
| --n3 | surface 强调/边框弱 | 输入底、分割弱线 | Web 偏冷白，移动偏灰 |
| --n4 | page/禁用底 | 内容区背景、禁用态底 | 两端明度不同 |
| --n5 | **边框（Web）/ 次级填充（移动）** | 表格线、分隔线 / 图标底 | ⚠️ 两端语义用途不同，勿跨端套用 |
| --n6 | 边框强调 / 占位符 | 输入边框、placeholder | 明度差异大 |
| --n7 | **边框（Web）/ 正文灰（移动）** | 分割线 / 次级文字 | ⚠️ 语义错位最重——Web 用边框场景，移动用文字场景 |
| --n8 | 次级文字 | 辅助文字、图标 | 两端色值差异大 |
| --n9 | 文字弱化 | 注释、禁用文字 | 差异大 |
| --n10 | 文字主体 | 标题、正文 | 两端都偏深 |

## 亮色色值对照（2026-08-07 从两端真源提取）

| Token | Web | 移动 | 差异 |
|-------|-----|------|------|
| --n1 | #FFFFFF | #FFFFFF | 同 |
| --n2 | #FAFAFA | #F5F7FB | 微 |
| --n3 | #F6F9FC | #E9ECF0 | 中 |
| --n4 | #F5F7FB | #DEE2E5 | 中 |
| --n5 | #EAEAED | #CED4E4 | **大**（边框 vs 填充） |
| --n6 | #DEE2E6 | #ADB5BF | **大** |
| --n7 | #CED4DA | #6C757D | **语义错位** |
| --n8 | #ADB5BD | #334155 | **大** |
| --n9 | #6C757D | #343A46 | 中 |
| --n10 | #334155 | #21253C | 微 |

## 暗色色值对照（两端一致，2026-08-07 已统一）

| Token | 值 | 语义 |
|-------|-----|------|
| --n1 | #1E293B | surface |
| --n2 | #243044 | elevated |
| --n3 | #334155 | border |
| --n4 | #121826 | page 最深 |
| --n5 | #1E293B | 同 n1 |
| --n6 | #334155 | 同 n3 |
| --n7 | #475569 | 弱文字 |
| --n8 | #94A3B8 | 次级文字 |
| --n9 | #CBD5E1 | 辅助文字 |
| --n10 | #F1F5F9 | 文字主体（白） |

## 使用守则（Agent 必须遵守）

1. **跨端组件**：同一语义（如"边框"）在两端分别用各端正确的 n 阶——Web 边框用 `--n6`，移动边框用 `--n3`，**不要因为同名就跨端照搬**。
2. **文字**：正文统一用各端 `--n10`（文字主体），次级文字 `--n8`；**移动端勿用 `--n7` 做边框、Web 端勿用 `--n7` 做文字**（语义错位 token，见上表）。
3. **暗色**：两端暗色 N 完全一致，可放心跨端复用。
4. 改任一色值 → 改该端 `template.css` 真源 → 本表同步 → 重跑该端 DESIGN-TOKENS 生成脚本。
5. 有跨端统一诉求（如品牌色/语义色）→ 用 `--primary/--suc/--warn/--err` 语义色，不要用中性色硬凑。
