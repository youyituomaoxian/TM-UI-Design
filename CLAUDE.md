# Claude Code 项目指令

> 本文件是 **Claude Code** 的入口——Claude Code 原生只读 `CLAUDE.md`，不读 `AGENTS.md`。
> 规范与生成流程的唯一权威入口 = `AGENTS.md`（Codex / Reasonix / Cursor / 其他认 AGENTS.md 的 Agent 共用），
> 本文件通过 `@AGENTS.md` 导入复用同一份指令，避免双份漂移（2026-08-13 建立）。

@AGENTS.md
