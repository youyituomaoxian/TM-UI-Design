# 弘讯设计系统 · 团队使用指南（人读版）

> 面向团队成员（设计师 / 前端 / 产品 / 任何要用 AI 生成界面的人）。
> 这份文档不要求你读懂 Agent 的执行细节——只要照着「用法」和「例子」用，就能让 AI 生成**风格统一**的页面与设计稿。
> 最后附「文件作用一览」，帮你理解这套系统由什么组成。

---

## 一、这套系统解决什么问题？

**痛点**：团队成员都用 AI 工具（如 Codex）生成界面，但——

- 每个人生成的页面**长得不一样**
- 同一个人**每次生成**风格都漂移
- 同一个产品**每个页面**都不像同一套

**根因**：AI 没有「统一的设计基准」，全靠自由发挥。

**方案**：弘讯设计系统把「品牌风格」做成了**可执行的机器资产**——

- **设计令牌**（颜色 / 字号 / 间距 / 圆角 / 投影）只有一套，全部写成变量
- **组件**（按钮 / 表格 / 表单 / 图表…）有标准样式，AI 只能用它，不能自造
- **生成规范 + 门禁**：AI 生成的页面要过自动检查（0 HIGH 才能交付），风格跑偏会被拦住

一句话：**只要让 AI「用弘讯设计系统」，它就能稳定地产出同一品牌风格的界面**。

---

## 二、两条使用路径

### 路径 A：用 AI 生成「视觉页面」（HTML 原型）——最常用

**适合**：快速出页面原型、评审、对接开发、内部演示。

**流程**（你只需要做第 1 步，剩下交给 AI）：

```
你说一句话需求 → AI 读设计系统规范 → 生成 HTML 页面 → 自动过门禁（风格合规）→ 浏览器打开即看
```

**怎么对 AI 说（话术模板）**：

> 用弘讯设计系统，生成一个「设备监控大屏」B 端页面，包含：顶部导航、左侧作业树、4 张 KPI 图标卡（设备总数/开机率/故障数/今日产量）、设备列表表格（数值列右对齐）、近 7 日产能柱状图。深色模式。

> 用弘讯设计系统，生成一个「工单管理」页面，表格 + 筛选 + 分页 + 新建工单表单弹窗。

**反面例子（会导致风格漂移，不要这样问）**：

> ❌ 帮我画个好看的设备管理页面。
> ❌ 做一个酷炫的运营大屏，配色随意发挥。

**产出物**：一个 `.html` 文件，浏览器打开就是完整页面；分享版可发给任何人看。

---

### 路径 B：用 AI 生成「UI 设计稿」（Ardot / Figma 类画布）

**适合**：需要原生设计稿交付（组件实例 / 变量绑定 / 文本样式）给设计师在 Ardot 里继续精修。

**流程**：

```
你说一句话需求 → AI 读设计系统组件映射 → 在 Ardot 画布搭建页面帧（用组件库 + 变量 + 文本样式）→ 截图自验 → 设计师手动收尾
```

**前提**（三者缺一就退回路径 A）：
1. 已安装并登录 **Ardot**
2. 可访问 Ardot 组件库文件（Web / 移动端各一个）
3. AI 已连接 Ardot（MCP 插件）

**没有 Ardot 或没有组件库文件怎么办？**
→ **不影响**——直接用路径 A 的 HTML 原型，它就是「设计稿等价物」（可预览、可评审、可标注、可交付开发）。Ardot 只是锦上添花的原生画布路径。

**例子**：

> 用弘讯设计系统，在 Ardot 里搭一个「移动端登录页」设计稿：顶部导航 + 手机号输入 + 验证码 + 登录按钮。

---

## 三、日常协同规则（团队约定）

1. **所有 AI 生成的页面，话术里必须带「用弘讯设计系统」**——这是统一风格的第一道闸。
2. **页面有标准框架，不要 AI 自由发挥版式**：
   - Web 后台 = 顶栏 72 + 左侧作业树 240（可折叠）+ 内容区 + 底栏 32
   - 移动端 = 状态栏 + 导航栏 + 内容滚动区 + 底部导航
3. **KPI 卡只有两种**：图标卡（标准版）/ 环形卡（环形版）——不要自造第三种。
4. **颜色只用规范里的令牌**，不允许 AI 硬编码任意色值（红色警示 #10B981 是移动端专用，Web 禁用）。
5. **数值列表格**：数值列右对齐（表头和数据一起）。
6. **生成后必须过门禁**（0 HIGH）——让 AI 自己跑校验，报错就修，不要跳过。

---

## 四、品牌定制（换色 / 暗色）

- **换品牌色**：告诉 AI「用 #2E7D5B 生成」——系统会自动推导整套协调配色（主色/悬停/禁用/图表色/中性色微调），无需人工调。
- **暗色模式**：告诉 AI「深色模式」——自动切换整套暗色令牌。
- **功能色（成功/警告/错误/运行）不随品牌变**——保证状态语义全公司一致。

---

## 五、部署与团队分发（两条路径）

设计系统仓库可以独立于任何项目使用：**设计系统（规范）放一边，你的项目放另一边**，AI 生成页面时引用规范、产物落你的项目。给团队分发有两种路径，任选其一（可并存）。

### 路径 A：GitHub 仓库克隆（推荐，团队 ≥2 人）

**部署（维护者做一次）**
1. 在 GitHub 建一个**私有**仓库（已建好：`youyituomaoxian/TM-UI-Design`）。
2. 本机进入设计系统目录，初始化并推送到远程（已完成；后续更新只需）：
   ```powershell
   git add .
   git commit -m "更新说明"
   git push
   ```
   > `.gitignore` 已配好：`node_modules/`、`dist/`、`output/`（历史归档）、`.reasonix/`（本机记忆）、`.workbuddy/` 本地内容会自动排除；**`.workbuddy/skills/`（workbuddy skill）会随仓库提交**。
3. 仓库为**公开**——成员无需加 Collaborators 即可 clone；如需限制访问可改回私有（Settings → Danger Zone → Change visibility）并加成员。

**使用（团队成员）**
1. `git clone https://github.com/youyituomaoxian/TM-UI-Design.git` 到本地任意位置（如 `D:\design-system`）。
2. 打开你的项目目录，让 AI 干活：
   - **codex / Claude Code**：在设计系统克隆目录（或项目里引用该路径）对话，`AGENTS.md` 自动加载，直接说需求即可。
   - **workbuddy**：打开项目 → 项目级 skill 自动生效（克隆目录内含 `.workbuddy/skills/`）。
   - **其他 Agent**：设环境变量 `TM_DESIGN_REPO=<克隆路径>`，或把克隆路径告诉它。
3. 生成页面：产物落**你项目**的 `output/`（AI 会按规范做），门禁 0 HIGH 后交付。

**规范更新（维护者 push 后）**
- 成员：`git pull` 拉取最新规范（改了什么看两端 `CHANGELOG.md`）。
- 这是路径 A 的最大优势：**规范一处更新，全员同步**。

### 路径 B：拷贝分发（团队 1 人 / 内网 / 不便用 Git）

**部署（维护者）**
1. 打包设计系统目录为 zip（**排除**大文件和本地内容以减小体积）：
   ```powershell
   # 排除：output/（历史归档）、node_modules/、.git/、.reasonix/、packages/*/dist/
   ```
2. 通过网盘 / 内网 / U 盘发给成员。
3. 成员解压到本地任意路径（如 `D:\design-system`）。

**使用（团队成员）**
- 与路径 A 完全一样：`AGENTS.md` 随目录生效（codex 零配置）、workbuddy 自动加载 skill、`TM_DESIGN_REPO` 指向解压路径。

**规范更新**
- 维护者重新打包发新版本（无版本控制是短板）。
- 建议成员本地 `git init` 跟踪变更，或维护者定期发布新包。

### 两路径对比

| 维度 | A：GitHub 克隆 | B：拷贝分发 |
|------|--------------|------------|
| 上手速度 | 需 git 基础 | 解压即用 |
| 规范更新 | `git pull` 一键 | 重新打包拷贝 |
| 版本历史 | ✓ 完整 | ✗ 无（可本地 init） |
| 适合场景 | 团队 ≥2、异地协作 | 单人、内网、快速起步 |
| 体积 | 仓库（不含大文件） | zip 按需裁剪 |

> **两种路径都可以先小范围试点**：先把仓库/压缩包给 1 个成员试跑，验证 codex/workbuddy 能正常生成 0 HIGH 页面后，再全员铺开。

---

## 六、文件作用一览（这套系统由什么组成）

### 根目录

| 文件/目录 | 作用 |
|-----------|------|
| `GENERATION-SOP.md` | **生成规范总纲**：AI 按它把一句话需求变成合规页面（选端→定结构→选组件→过门禁）。人不用读，AI 读。 |
| `CHART-SPEC.md` | **图表规范**：折线/柱状/横向柱状/环状/分组柱状的画法、颜色、交互、动画。AI 生成图表时读。 |
| `DARK-MODE.md` | **暗色模式规范**：暗色令牌怎么定义、怎么切换、色板参考。 |
| `VISUAL-SPEC.md` | **视觉规格总览**：品牌色/字阶/间距/圆角/投影/动效的核心数值速查。 |
| `README.md` | 仓库总说明。 |
| `USAGE-GUIDE.md` | 使用指南（含 Ardot 资产表）。 |
| `USAGE.html` | 使用指南的 HTML 网页版（浏览器打开即看）。 |
| `BLOCKED.md` / `LICENSE` | 占位说明 / 开源协议。 |
| `PROGRESS.md` / `HANDOFF.md` | 开发进度 / 交接笔记（维护者用）。 |
| `ci-local.js` | **一键全量自检**：`node ci-local.js` 跑两端克隆源门禁 + token 一致性（86 项，全绿才算健康）。 |
| `brand-color-engine/` | **品牌色引擎**：换任意品牌色自动生成整套配色（亮色/暗色/图表色），AI 换色时调用。 |
| `packages/` | **上线参考实现（可选，可忽略）** + **template.css 构建源**。两层：① `packages/web-ui/src/styles/` 四件 CSS = `template.css` 的**构建真源**（`npm run build:template` 合并生成——改视觉样式必须改四件再 build，这是规范的一部分）；② React/Taro 组件库与脚手架（`web-ui`/`mobile-ui` 的 src/）是**可选参考实现**——恰好用 React/Taro 的团队直接复用现成组件，异栈团队**完全不用管**（上线用什么由使用者决定）。 |
| `scripts/` | git 钩子：`git-hooks/` + `setup-hooks.sh`（提交前自动跑门禁的钩子安装脚本）。 |
| `output/` | **历史产物 / 示例归档**（不随设计系统分享）：早期生成的业务示例页 + `share/` 旧分享版。**你的新页面不落这里**——生成页一律进「你的项目」的 `output/`（见路径 A）。 |

### Web 端（`弘讯web端design-system/`）

| 文件 | 作用 |
|------|------|
| `template.css` | **Web 视觉真源**：全部组件样式（按钮/表格/表单/图表/框架…）+ 设计令牌变量。AI 页面 link 它，改它 = 全站变风格。 |
| `tokens.json` | **设计令牌机器格式**：颜色/字号/间距/圆角/投影的结构化数据（template.css 与它保持一致，有门禁）。 |
| `components.json` | **组件规格契约（机器可读）**：每个组件（按钮/输入/表格/标签/分页…）的尺寸、颜色、圆角、Ardot 映射——驱动门禁，AI 生成组件前读它。 |
| `RULES.md` | **Web 规则**：布局黄金法则、组件选型、色彩/字号决策树、自造类守则、防溢出清单。AI 生成时遵守。 |
| `MASTER.md` | **Web 主规范**：设计稿提取的总规范（组件骨架、字号体系、颜色体系）。 |
| `DESIGN-TOKENS.md` | 设计令牌人读版（设计师对照）。 |
| `components.md` | 组件规格人读版（含 Ardot 搭建指引）。 |
| `validate-spec.js` | **门禁脚本**：`node validate-spec.js 页面.html` 检查风格合规（0 HIGH 才许交付）——风格统一的技术保障。 |
| `page-template.html` | **Web 克隆源（纯框架）**：AI 生成新页面从这里开始（顶栏/侧栏/底栏框架），克隆后只填内容。 |
| `agent-starter.html` | 极简起步模板（自由布局场景用）。 |
| `pages/` | 页面类型规格已并入 `RULES.md §1.1b`（2026-08-06，两端同步）；目录已空。生成页面按 `RULES.md §1.1b` 直接自建，不再查找近似模板。 |
| `pages/examples/` | 品牌示例页已删除（2026-08-06 用户拍板）；需要品牌演示时用 `brand-color-engine/examples/gen-examples.js` 重新生成。 |
| `icons/` | 图标资产。 |
| `scripts/` | `generate-design-tokens-md.js`：从 `tokens.json` 一键生成人读版 `DESIGN-TOKENS.md`（改了令牌后跑它保持文档同步）。 |
| `CROSS-PLATFORM-COLORS.md` | 跨端颜色对照（Web/移动功能色差异）。 |
| `CHANGELOG.md` | 变更记录。 |

### 移动端（`弘讯移动端design-system/`）

与 Web 端**同构**（文件职责一一对应），只是移动端专属：

| 文件 | 作用 |
|------|------|
| `template.css` / `tokens.json` / `components.json` / `RULES.md` / `MASTER.md` / `DESIGN-TOKENS.md` / `components.md` / `validate-spec.js` | 同 Web 端对应文件（移动端专属的 m 前缀组件：mbtn/mtag/mlist/msearch…、375 手机壳、底部导航）。 |
| `page-template.html` | **移动端克隆源（纯框架）**：状态栏 + 导航栏（标题居中）+ 内容滚动区 + 底部导航。 |
| `pages/` | 页面类型规格已并入 `RULES.md §1.1b`（2026-08-06，两端同步）；目录已空。 |
| `icons/` + `scripts/` | 图标资产 + `generate-design-tokens-md.js`（tokens.json → DESIGN-TOKENS.md 生成脚本）。 |
| `design-system-showcase.html` | 移动端规范展示（移动端展示页源，同 Web 展示页角色）。 |

### 规范展示页（各端目录内，随设计系统走）

| 文件 | 作用 |
|------|------|
| `弘讯web端design-system/web规范展示_框架版_*.html` | **Web 规范活体展示**：所有视觉特征（色板/字阶/组件/图表/暗色）一目了然。想快速了解 Web 风格就看它（旁边同名 `_分享版.html` 零外链，直接发给任何人）。 |
| `弘讯移动端design-system/移动端规范展示_*.html` | **移动端规范活体展示**（同样带 `_分享版.html`）。 |
| `弘讯移动端design-system/design-system-showcase.html` | 移动端早期全量展示页（2026-07-07，内联旧样式，已过时；以 `移动端规范展示_*.html` 为准）。 |

---

*祝大家生成愉快，风格统一。*

## 四、仓库更新到最新（已 clone 过的成员）

设计系统会持续迭代（视觉纠正/规范沉淀），成员本地 clone 的副本需要拉取最新：

```powershell
# 1. 先看本地有没有未提交的修改
git status

# 2a. 干净状态 → 直接拉取最新
git pull

# 2b. 有本地修改但不想提交 → 暂存后拉取，再恢复
git stash
git pull
git stash pop

# 2c. 有本地提交 → 变基到最新（避免多余的合并提交）
git pull --rebase
```

**注意事项**：
- 仓库默认分支是 **main**（`git pull` 自动跟随）。
- 若改过规范文件（template.css / RULES.md / validate-spec.js 等）且与最新推送冲突，`git pull` 会提示冲突——手动解决后 `git commit`，或 `git checkout -- <文件>` 放弃本地改动。
- 拉取后如果规范/组件有更新，重新生成页面时用最新真源（生成器 `brand-color-engine/examples/gen-examples.js` 输出即最新规范产物）。
