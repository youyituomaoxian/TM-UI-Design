# 待修复问题清单（2026-08-12 布局模式库任务前收集 · 已闭环）

> 来源：布局多样化测试（4 个测试页）+ 门禁实测 + 规范核查过程中发现的问题。
> 状态：✅ **7 项全部已修复**（2026-08-12 布局模式库任务中/后逐一处理完毕，本文件留档归档）。
> 证据出处：`output/布局多样化测试/`（测试页与截图）+ 对应 git 改动。

## 1. [门禁 HIGH] 分页器门禁 2500 字符窗口缺陷（table.pager.required）— ✅ 已修复
- **修复**：`validate-spec.js` `checkTablePager` 由「表格后 2500 字符窗口」改为「本表格结束后 → 下一个表格前」区间查找 `.pager`（pager 恒紧跟所属表格，列多/行多不再误报，多表格页互不串扰）。
- **验证**：工单列表页（pager 距表格 2876 字符）误报 HIGH 消失，全部 6 测试页 0 HIGH。

## 2. [规范] ≤5 行表格尾部无统一处理 — ✅ 已修复
- **修复**：RULES §1.1b 列表页「分页器规则」定稿：>5 行带完整 `.pager`；≤5 行短表格尾部统一「共 N 条」（复用 `.pager .pg-tot`）；详情页参数表等明细表豁免。
- **验证**：页4 详情页参数表已加「共 5 条」。

## 3. [文档] 「布局决策 5 问」vs「6 问」不一致 — ✅ 已修复
- **修复**：两端 RULES 软规则清单「布局决策 5 问」→「6 问（任务/优先级/关系/倾向/论证/高度）」，与 GENERATION-SOP 一致。

## 4. [流程] page-template 克隆残留 demo `<style>` 块 — ✅ 已修复
- **修复**：SOP 步骤 4 克隆清单补第 6 件「删 head 展示样式 `<style>` 块（保留内联 :root 同步副本）」；6 个测试页已删（MED 27-34 → 4-11）。

## 5. [流程] output 子目录克隆 link 相对路径层级陷阱 — ✅ 已修复（无需改脚手架）
- **修复**：`scripts/new-page-web.js` L50 本就以 `path.relative(outDir, …)` 自动按落盘目录算层级（脚手架正确）；SOP 步骤 4 第 2 件补「相对层级按落盘目录计算（output 直下 `../` / 子目录 `../../`）」说明。

## 6. [工具] headless 截图 CSS 动画需 --timeout 真实等待 — ✅ 已修复
- **修复**：两端 RULES §7.8 / §10.9 视觉自检截图 `--virtual-time-budget` → `--timeout=3500`（真实等待动画播完）。

## 7. [规范] col-* 多卡等高与定高内容混排 — ✅ 已修复（留档）
- **修复**：真源 `utilities.css` `.col-* > .card{min-height:0}` → `auto`（兑现 RULES §4.4「内容超高卡 min-height:auto 保底」文档承诺，表格/时间轴不再溢出卡片）。
- **留档说明**：§4.4 文档本已写明 min-height:auto 语义，真源现已对齐，**无需额外补 §4.4b**——文档与真源一致即防回归。

---

## 附：本轮新增的后续项（均已处理完毕）

| 项 | 处理 |
|---|---|
| 生成器 `gen-examples.js` 双模式 | ✅ brandCover 改「亮 :root + 暗 :root[data-theme="dark"]」双块 |
| components.json 登记 breadcrumb 契约 | ✅ 已登记（32/N5 底/› 分隔/间距 16，无门禁） |
| sun/moon 图标入库 | ✅ icons/moon.svg + sun.svg + icons.md 登记（B 端特有 14→16，共 198） |
| 移动端顶栏暗色切换按钮 | ⏭️ 不做（移动端特性，用户拍板） |
